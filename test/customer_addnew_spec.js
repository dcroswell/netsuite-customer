/**
 * Created by Don on 24/05/2015.
 */
var should = require("should");
var C = require("../models/customer");
var NC = require("../libs/newcustomer");

describe("NewCustomer", function() {

/*    describe("defaults", function() {
        var c = C({email: "don@greenbilling.com"});

        before(function() {
            newcustomer = NC.NewCustomer(c)
        });
        it("is successful", function() {
            newcustomer.result.success.should.equal(true);
        });

        it("creates a customer");
        it("gets the customers internalid");
        it("logs the new customer");
    });

    describe("an empty email or null email", function() {
        it("is not successful");
        it("tells user that email is required");
    });

    describe("email already exists", function() {
        it("is not successful");
        it("tells user that email is already used");
    });*/

});
